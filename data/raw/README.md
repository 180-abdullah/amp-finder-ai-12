# Raw data

Place user-obtained source files here:

- `apd_positive.fasta` — APD3 or current APD FASTA downloaded from the official APD site.
- `uniprot_negative_parents.fasta` — reviewed UniProt proteins used only as parents for putative-negative fragments.

These files are intentionally excluded from the repository. See `docs/DATA_ACQUISITION.md`.
