# Model artifacts

- `demo_baseline_rf.joblib` is synthetic and exists only for installation/UI smoke tests.
- `baseline_rf.joblib` is created from the user-prepared APD/UniProt dataset.
- `esm2_logreg.joblib` is created after ESM-2 embedding extraction and training.

The app automatically prefers real artifacts when they are present.
